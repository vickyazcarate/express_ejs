const express = require('express');
const router = express.Router();

usuariosController = require('../controller/userController');

router.get('/user', function(req, res) {
    // Your route logic goes here
  });

module.exports = router;